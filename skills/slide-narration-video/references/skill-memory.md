# Skill memory

Updated: 2026/08/01 01:27

運用で得た落とし穴・成功パターンを追記する。秘密情報や環境固有のトークンは書かない。

- 画面は要約しすぎない。主要バレット＋インデント下位項目・具体例で厚くし、ナレーションを追える密度を好む（悪いのは階層なし並列や無関係詰め込み）。
- 概念・比較・フローの枚では図・表を積極採用する。箇条の厚さと両立させる（図のために薄くしない）。
- Mermaid は Marp 生フェンスに頼らない。`@mermaid-js/mermaid-cli`（`mmdc`）で SVG/PNG 先レンダ → `![](...)` 埋め込みが最も確実。
- Mermaid ノード内の改行は `\n` ではなく `<br/>`（`\n` はリテラル文字として残ることがある）。
- ローカル SVG/PNG を埋め込む PNG 書き出しでは Marp CLI に `--allow-local-files` が必須（省略すると図が欠ける）。
- 動きのないスライドショーは Remotion より ffmpeg 静止画結合が速い。fps は 15 を候補に。書き出し後は ffprobe／1 フレーム抽出で NAL 破損を確認する。
- Remotion は同時複数 render や高い concurrency で mux 破損しうる。exit 0 でも再生不能になりうる。
- 数式は Marp `math: mathjax` を採用（PNG 書き出しで数式が焼けることを実測）。KaTeX は CDN フォント依存でオフライン崩れやすい。先レンダ SVG は MathJax が通る限り不要。
- 書き出し後は必ず PNG を目視する。`flowchart TD` や生 `$E=mc^2$` が見えたら失敗としてやり直す。
- 下端の `.note` や長いコードは端欠けしやすい。PNG 目視ゲート（工程 2.5）を省略しない。`overflow: hidden` は修正ではない。
- 画像を枠に収めるとき両軸固定で押しつぶさない。`max-width: 100%; height: auto;` または `object-fit: contain`。
- 出典ソースに説明用の図があるとき、文字再現が非効率なら図を抽出してスライドへ。出典案件は先頭スライドのタイトル〜バレット間に参考タイトル＋URL。
- 未知の英語固有名詞を自動ローマ字読みしない。Gigatoken 等は `pronunciation.yml` に明示（TTS 直前変換）。`narration` SoT と画面英語は維持する。
- 解説で置いていかれやすい内容は monologue より dialogue（会話劇としてつまずき・早合点・訂正を置く）が効く。聞き手を優等生要約係にすると視聴者が置いていかれる。
- dialogue の各台詞へ cognitive-rhythm を機械適用すると、綺麗すぎて劇場口調と干渉する。dialogue では会話劇を優先し、cognitive-rhythm は場面接続のみ。
- 「ちょっと自信ない」等のメタ宣言や、分からないフリで分かった喋りは違和感の元。具体的な早合点・噛みつきで返す。
- 下枠の「○○（解説）：制作意図の要約」カードは出さない。字幕は現在台詞のみ。概念はスライド本文側で担保する。
- 劇場レイアウトでは解説役を左下・聞き手を右下。台詞字幕と立ち絵が被らないよう安全域を先に取る。口パクは開閉2枚で足りる。
- 対話カードは最小高・行間・下端安全域を先に確保する。文字重なりは PNG だけでなく動画プレビューでも確認する。
- PowerShell 5.1 で JSON を扱う生成工程では、UTF-8 の入出力エンコーディングを明示する（既定コードページで日本語キューが壊れる）。
- 話者交替には短い pause を置く。無音をゼロにすると会話の区切りと理解の間が消える。
- cue 時刻は原稿の推定ではなく、発話ごとの TTS 実測長を累積して解決する。
- 対話カードの表示期間は、対応する発話の実測区間と一致させる。
- dialogue の書き出しはページ単位（または動き切れ目単位）クリップ→結合の方が、立ち絵口パクの差し替えがしやすい。
- 2026/08/05: 劇場 dialogue では密度・理解確認・前提・メタ分離を `voicevox-theater-video/references/dialogue-density.md` に従う。メタ指示語を本文へ入れない。
- 2026/08/09: 依存先の責務を重ね掛けしない。JT はスライド論理、dialogue-writing は dialogue の台詞、cognitive-rhythm は monologue と場面接続、劇場プロファイルは密度・合成を担当する。参照文書の不自然な語も生成条件へ混入し得るため、原稿だけでなく参照語彙も点検する。
