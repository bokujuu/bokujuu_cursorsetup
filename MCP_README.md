# Cursor MCP サーバー設定ガイド

## 概要
このプロジェクトでは、Cursor エディタで利用可能な MCP（Model Context Protocol）サーバーを設定しています。  
APIキー不要で利用できる便利なサーバーを中心に構成されています。

## 設定済み MCP サーバー

### **既存設定**

#### **Context7**
- **機能**: ライブラリドキュメントの検索・取得
- **用途**: 最新のライブラリ情報、APIリファレンスの参照
- **APIキー**: 不要

#### **Excel MCP**
- **機能**: Excel ファイルの読み書き・編集・フォーマット
- **用途**: スプレッドシート操作、データ分析、レポート作成
- **APIキー**: 不要

#### **Playwright MCP**
- **機能**: ブラウザ自動化・テスト実行
- **用途**: Webスクレイピング、UI テスト、ブラウザ操作
- **APIキー**: 不要

### **構成サーバー**

#### **Filesystem MCP**
- **機能**: ローカルファイルシステムへのアクセス
- **用途**: ファイル読み書き、ディレクトリ操作、ファイル管理
- **APIキー**: 不要

#### **Memory MCP**
- **機能**: セッション間でのデータ永続化
- **用途**: 作業状況の保存、一時データの管理
- **APIキー**: 不要

#### **Serena MCP**
- **機能**: AIアシスタント機能の拡張
- **用途**: コード生成、ファイル操作、タスク実行の自動化
- **APIキー**: 不要
- **特徴**: Claude Codeと同様の機能をCursorで利用可能

## 設定方法

### **1. 設定ファイルの配置**
```powershell
# 提供された mcp_enhanced.json を以下の場所にコピー
Copy-Item mcp_enhanced.json $HOME\.cursor\mcp.json
```

### **2. Cursor の再起動**
設定適用のため Cursor を再起動してください。

### **3. 動作確認**
Cursor のチャットで以下を試してください：
```
@excel test_workbook.xlsx の内容を確認してください
@filesystem プロジェクト内のファイル一覧を表示してください
@git 現在のブランチ状況を教えてください
```

## 実用例

### **Excel ファイル操作**
```
@excel temp フォルダ内の Excel ファイルを分析してください
```

### **ファイル管理**
```
@filesystem temp フォルダ内の Python ファイルを確認してください
```

### **データ保持（簡易メモ）**
```
@memory 今日の作業メモを保存してください
```

### **ブラウザ自動化**
```
@playwright 指定したWebサイトをスクレイピングしてください
```

### **AIアシスタント機能**
```
@serena プロジェクトの構造を分析して改善提案をしてください
```

## トラブルシューティング

### **よくある問題**

#### **MCP サーバーが認識されない**
1. Cursor の再起動
2. 設定ファイルの JSON 形式確認
3. Node.js がインストールされているか確認

#### **NPX コマンドエラー**
```powershell
# Node.js の更新
npm install -g npm@latest
```

#### **権限エラー**
```powershell
# PowerShell を管理者権限で実行
Set-ExecutionPolicy RemoteSigned -Scope CurrentUser
```

## 更新履歴
- **2024年**: 初期設定（Context7、Excel、Playwright）
- **2024年**: APIキー不要サーバー追加（Filesystem、Memory）
- **2025年**: Serena MCPサーバー追加（AIアシスタント機能拡張）

---
**注意**: 各MCPサーバーの詳細な使用方法については、Cursor のチャット機能で直接質問してください。
